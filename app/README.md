# app/

The Relatively Normal website and, later, the wardrobe tool.

**Empty on purpose.** Nothing is being built here yet.

When it does start, this folder holds the application code: the site that
publishes posts from `content/posts/`, and the tool that applies the systems in
`frameworks/` against the brand data in `brands/`.

Until then, the project is writing and research only — no application code
anywhere in the repo.
